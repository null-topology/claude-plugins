## Objective
Return the number of occurrences of the exact word "red" in the supplied text.

## Context and Evidence
The entire input is: "red green red blue". Match lowercase whole words only.

## Scope Boundary
Use only the text above; no files, network access, further delegation or mutations.

## Acceptance Criteria
Every matching word is counted once.

## Return and Stop
One line: the count and the one-based word positions. If the matching rule is ambiguous for this input, say so instead of changing it.
